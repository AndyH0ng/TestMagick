# Sample Output Snapshot

This folder contains output artifacts generated from:

- `data/sample_problems.yaml`

Generation command:

```bash
./run build --input data/sample_problems.yaml --out out
```

Included files:

- `exam.pdf`
- `answer.pdf`
- `exam.typ`
- `answer.typ`
